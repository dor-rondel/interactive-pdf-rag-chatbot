import React from 'react';

/**
 * Header component that displays the main application title.
 * Simple presentational component with styled heading.
 */
const Header = () => {
  return <h1 className="text-2xl font-bold">Interactive PDF RAG Chatbot</h1>;
};

export default Header;
