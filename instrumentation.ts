/**
 * Next.js instrumentation entrypoint.
 *
 * Next.js discovers this file by its path/name, so it must remain at the
 * project root. The implementation lives in `app/lib/langfuse/instrumentation`.
 */
export { register } from './app/lib/langfuse/instrumentation';
